{
  "name": "next-firebase-template",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently 'firebase emulators:start' 'yarn workspace functions watch'",
    "predeploy": "bash ./scripts/predeploy.sh",
    "deploy": "firebase deploy"
  },
  "workspaces": [
    "packages/*"
  ]
}
